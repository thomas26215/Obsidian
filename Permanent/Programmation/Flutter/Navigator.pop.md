Le widget `Navigator.pop(context)` ferme la page courante et fait revenir à la page précédance.

> [!Example]
> ```dart
> onTap: () {
> 	Navigator.pop(context);
> }
> ```

---

[[onTap]]
[[Context en Flutter]]